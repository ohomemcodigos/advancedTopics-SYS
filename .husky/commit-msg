@echo off
node -e "const { execSync } = require('child_process'); try { execSync('npx --no commitlint --edit ' + process.argv[1], { stdio: 'inherit', env: process.env }); } catch (e) { process.exit(1); }" %1